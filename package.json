{
  "name": "mud-pi",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun --watch src/main.ts",
    "tui": "bun run src/main.ts --tui",
    "telnet": "bun run src/main.ts --telnet",
    "web": "bun run src/web/server.ts",
    "web:dev": "bun --hot src/web/server.ts",
    "logs:ai": "bun run src/diagnostics/analyze-ai-logs.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.3",
    "@earendil-works/pi-tui": "^0.80.10"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
